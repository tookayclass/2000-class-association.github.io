---
label: Event
hide_body: false
fields:
- type: text
  name: title
  label: title
- type: datetime
  name: date
  label: date
  config:
    date_format: DD/MM/YYYY
    export_format: YYYY-MM-DD
    required: true
- type: datetime
  name: date_end
  label: date_end
  config:
    date_format: DD/MM/YYYY
    export_format: YYYY-MM-DD
- type: text
  name: venue
  label: venue
- name: attachments
  type: field_group_list
  fields:
  - name: path
    type: file
    config:
      maxSize: '5'
    label: Document
    description: PDF document or image file
  - name: title
    type: text
    config:
      required: true
    label: Title
    description: Title or short description
  config:
    min: '0'
    max: '10'
    labelField: title
  label: Attachments
pages:
- _events/test-event.md
- _events/inland-championships-2019.md
