name: Greet Everyone
on: [push]

jobs:
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: docker://continuumio/miniconda3:4.7.10-alpine
    - run: |
        echo ${GITHUB_WORKSPACE}
